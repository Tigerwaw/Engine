#pragma once

namespace Math
{
	constexpr float PI = 3.14159f;
	constexpr float PI_Half = (PI / 2);
	constexpr float DEGREES_TO_RADIANS = (PI / 180);
	constexpr float RADIANS_TO_DEGREES = (180 / PI);
}